apiVersion: v1
kind: Service
metadata:
  name: { { .Values.name } }
spec:
  type: ClusterIP
  selector:
    service: { { .Values.name } }
  ports:
    - name: db
      port: { { .Values.port } }
      targetPort: { { .Values.port } }
