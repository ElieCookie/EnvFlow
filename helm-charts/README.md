# helm-charts

Charts for EnvFlow environments live **in this repository**. `sun rise` symlinks this directory to `~/.envflow-ephemeral/helm-charts` so DevSpace / Helm can resolve chart paths consistently.

Add chart subfolders here as you build out (`pg`, `redis`, and service charts).
